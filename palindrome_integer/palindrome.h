#ifndef PALINDROME_H
#define PALINDROME_H

/*
 * Checks if an unsigned long integer is a palindrome.
 * @n: The number to check.
 * Return: 1 if n is a palindrome, 0 otherwise.
 */
int is_palindrome(unsigned long n);

#endif /* PALINDROME_H */